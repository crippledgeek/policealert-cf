spring:
  application:
    name: police-alert-config-server

  cloud:
    config:
      server:
        git:
          uri: https://github.com/crippledgeek/police-alert-config.git
      fail-fast: true

server:
  port: 8888
